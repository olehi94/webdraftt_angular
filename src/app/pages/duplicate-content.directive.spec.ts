import { DuplicateContentDirective } from './duplicate-content.directive';

describe('DuplicateContentDirective', () => {
  it('should create an instance', () => {
    const directive = new DuplicateContentDirective();
    expect(directive).toBeTruthy();
  });
});
